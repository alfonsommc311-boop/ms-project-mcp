# Dependencias del bot de mensajería.
# Instala con:  pip install -r bot/requirements.txt

# Cerebro (Claude / Anthropic) + cliente MCP
anthropic>=0.49.0
mcp>=1.0.0

# Carga de variables desde .env (opcional pero recomendado)
python-dotenv>=1.0.0

# Canal Telegram (funcional)
python-telegram-bot>=21.0

# --- Canales opcionales (descomenta al implementarlos) ---
# Slack (Socket Mode)
# slack_bolt>=1.18.0
# WhatsApp / webhooks
# fastapi>=0.110.0
# uvicorn>=0.29.0
# Email async (alternativa a la stdlib)
# aioimaplib>=1.0.1
# aiosmtplib>=3.0.0
